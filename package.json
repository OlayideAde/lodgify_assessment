{
  "name": "lodgify_assessment",
  "version": "1.0.0",
  "description": "This repo contains test cases for the lodgify QA engineer assessment",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open --env testEnv=local",
    "run-tests": "cypress run --browser chrome",
    "run-prod-tests": "cypress run --browser chrome --env testEnv=prod",
    "run-staging-tests": "cypress run --browser chrome --env testEnv=staging"
  },
  "author": "Olayide A.",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.14.2"
  },
  "dependencies": {
    "chai": "^5.1.1",
    "faker": "^5.5.3",
    "mocha": "^10.0.0",
    "mochawesome": "^7.1.3"
  }
}
